spring:
  application:
    name: provider
  profiles:
    active: dev
  cloud:
    nacos:
      server-addr: localhost:8848
      config:
        username: nacos
        password: nacos
      discovery:
        username: nacos
        password: nacos